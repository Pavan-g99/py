# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
pr:
- azure-pipelines
stages:
  - stage: stage1
    jobs:
     - job: job1
       continueOnError: true
       steps:
         - script: exit 1
     - job:
     
       condition: eq(dependencies.job1.result,'SucceededWithIssues')
       steps:
         - script:  echo "job2"

    
  
     
 
       
   
       
   

          
   

